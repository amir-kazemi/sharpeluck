# data lives on scratch via the ./data symlink -- never in git
data
*.parquet
*.zip
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
node_modules/
dist/
.env
node_modules/.cache
# Batch scripts name a specific cluster's account and partition; they stay local.
slurm/

# ...except the handful of served artefacts the deployed image ships with.
!results/
!results/**
